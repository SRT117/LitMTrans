# LitMTrans 贡献指南

感谢参与 LitMTrans。欢迎提交代码、文档和问题反馈。提交前如果方便，尽量让修改范围清楚、结果容易复现，并确认没有带入本机数据。

## 开发检查

```powershell
$env:QT_QPA_PLATFORM = "offscreen"
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
.\.venv\Scripts\python.exe -m compileall -q .
```

行为变更如果方便，欢迎补充或更新测试；暂时没有条件测试时，在 PR 中说明即可。请不要提交 API 密钥、访问令牌、对话记录、原始文献、解析结果、模型、构建目录或第三方二进制文件。

## 代码与文档

- 使用 UTF-8，保留现有中文界面的语言一致性。
- 注释只解释公开 API、非直观约束、隐私/安全边界或算法理由；不保留调试过程和版本迭代记录。
- 新增第三方依赖时，最好顺手说明许可证、来源、用途及二进制分发要求。
- 不要以“免费网页接口”替代经授权的服务 API。

## 提交问题

问题报告如果能包含复现步骤、预期行为、实际行为、系统/Python 版本及已脱敏日志，会很有帮助。请不要附上真实密钥或无权公开的文档。
